<%= simple_form_for @user, url: followings_path, method: :post, remote: true do |f| %>
  <div><%= hidden_field_tag :followee_id, @user.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
